import javax.swing.*;

class CoverageWindow extends JFrame {
	public CoverageWindow() {
		super("Adversarial Coverage Simulator");
		// initialize the window
		this.setSize(500,400);
		this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	}
}